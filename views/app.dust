<!DOCTYPE html>
<html>
    <head>
        {> header name="{title}" /}
        <link rel="stylesheet" href="/stylesheets/app.css">
    </head>
    <body>
        <div class='container'>
            {> inputUserTasks /}
            {> viewerUserTasks /}
            {> footer /}
        </div>
    </body>
</html>