<!DOCTYPE html>
<html>
    <head>
        {> header name="{title}" /}
        <link rel="stylesheet" href="/stylesheets/app.css">
    </head>
    <body>
        <div class='container'>
            {> inputUserTasks /}
            {> viewerUserTasks /}
            <div class='footer'>
                {> footer /}
            </div>
        </div>
    </body>
</html>